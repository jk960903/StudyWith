package 스킬트리;

public class Main {
	public static void main(String[] args) {
		String skill="CBD";
		String[] skill_trees= {"BACDE", "CBADF", "AECB", "BDA"};
		for(int i=0; i<skill_trees.length; i++) {
			if(skill_trees[i].contains(skill)) {
				System.out.println("!");
			}
		}
		
	}
	public static int solution(String skill, String[] skill_trees) {
        int answer = 0;
      	char[] skill1=skill.toCharArray();
        String[] result=new String[skill_trees.length];
        for(int i=0; i<skill_trees.length; i++){
            char[] tempskill=skill_trees[i].toCharArray();
            StringBuilder builder=new StringBuilder();
            int index=0;
            for(int j=0; j<tempskill.length; j++){
                if(index==skill.length()){
                    break;
                }
                if(skill1[index]==tempskill[j]){
                    builder.append(tempskill[j]);
                    index++;
                }
            }
            result[i]=builder.toString();
        }
        for(int i=0; i<result.length; i++){
            if(result[i].length()==0){
                answer++;
            }else if(result[i].equals(skill)){
                answer++;
            }else if(skill.contains(result[i])){
                answer++;
            }
        }
        return answer;
   }
}
